#ifndef TYPE_H
#define TYPE_H

enum Command
{
    boot_start = 40,
    boot_download,
    boot_end,
    boot_app_mode,
    boot_bootloader_mode_require,
    boot_bootloader_mode,
};

#endif // TYPE_H
